import React, { useState } from "react";
import axios from "axios";
import "./AddPost.scss";

const AddPost = (props) => {
  return (
    <React.Fragment>
      <div className="row">
        <h4>AddPost Component</h4>
      </div>
    </React.Fragment>
  );
};

export default AddPost;
